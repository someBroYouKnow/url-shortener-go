events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name localhost;

        location / {
            root /usr/share/nginx/html; 
            index index.html index.htm;
        }

        # Example: Proxy requests to a backend service (e.g., your NestJS app or Redis)
        # location /api {
        #     proxy_pass http://your_nest_app:3000; # If you had another service named 'your_nest_app'
        #     proxy_set_header Host $host;
        #     proxy_set_header X-Real-IP $remote_addr;
        # }

        # If Nginx needed to interact with Redis
        # location /redis-status {
        #     # You'd typically use a proper Redis client for this, Nginx isn't a direct Redis client.
        #     # This is just illustrative of Nginx being aware of the Redis service.
        #     return 200 "Redis is available at redis:6379";
        # }
    }
}